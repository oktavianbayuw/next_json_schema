import React from "react";

const Header = () => {
  return (
    <header className="navbar navbar-dark bg-dark">
      <div className="container">
        <span className="navbar-brand" style={{ float: 'right' }}>
          JSON Schema Validator
        </span>
      </div>
    </header>
  );
};

export default Header;
